This is well structured and close to production-ready. It is clear, scoped correctly, and aligned with the prior security phase. Replit should be able to execute against this without ambiguity.

There are only a few targeted refinements worth making to avoid implementation drift or future rework:

⸻

1) Tighten MFA secret handling (important)

Right now:

mfa_secret

This should not be stored as plain text.

Refinement:
Specify that the MFA secret must be encrypted at rest, not just stored.

Add:
	•	mfa_secret_encrypted (or explicitly state encryption requirement)

Also clarify:
	•	backup codes are hashed (one-way), not encrypted

⸻

2) Clarify MFA enforcement timing (avoids UX bugs)

Current wording:

Login is blocked until MFA is configured when policy requires it

This can cause lockouts if not handled carefully.

Refinement:
Clarify:
	•	user is allowed through password step
	•	then forced into MFA setup before accessing app

⸻

3) Strengthen export security (important for GDPR)

Add one explicit rule:
	•	export files must be:
	•	time-limited (expiry)
	•	single-use OR short-lived signed URL
	•	access-controlled (user/company match)

Right now it’s implied but not explicit.

⸻

4) Clarify deletion semantics (prevents data integrity issues)

Add one line:
	•	deletion must be idempotent and safe to retry

This matters operationally.

⸻

5) SSO readiness — add one constraint (prevents future rewrite)

Add:
	•	email must remain the primary unique identifier
	•	external_id is mapped, not replacing email

This avoids identity conflicts later.

⸻

6) Add audit requirement for MFA bypass/recovery

You already log MFA actions, but explicitly include:
	•	backup code usage must be logged as a security-sensitive event

⸻

7) Minor but valuable: async jobs resilience

For export + deletion:

Add:
	•	jobs must be resumable or retryable if interrupted

⸻

Final Improved Version (copy/paste ready)

Below is your spec with those refinements integrated, keeping your structure intact:

⸻

#44 - Identity Hardening, SSO Readiness & GDPR

What & Why

Extend the security foundation into identity maturity and regulatory readiness. Introduces TOTP-based MFA with enforcement policies, prepares the auth architecture for future SSO/SAML integration without overbuilding, and delivers GDPR-aligned data rights tooling (export, deletion, consent traceability).

This reduces account takeover risk, makes enterprise onboarding feasible, and ensures legal defensibility. Builds directly on the auth, RBAC, tenant isolation, and audit logging established in Task #12.

⸻

Done looks like

Users can enable TOTP MFA via an authenticator app; backup/recovery codes are generated (shown once, hashed at rest)

MFA secrets are stored encrypted at rest; backup codes are stored as one-way hashes

MFA is enforced at login once enabled; recovery flow allows backup code as fallback (single-use, invalidated after use and audit logged)

Company admins can set an MFA policy: optional / admin-required / all-required

When MFA is required by policy, users must complete MFA setup immediately after password authentication before accessing the platform

Identity provider model exists in the database (SAML/OIDC-ready); user model supports external_id and identity_provider_id while retaining email as the primary unique identifier

Auth service separates identity verification from session creation so additional providers can be added without refactoring login flow

Stub SSO endpoints exist (initiate + callback) with no regression to existing password login

Company admin can trigger a structured data export (JSON bundle) covering metrics, reports, evidence metadata, and user/account data — excluding secrets

Users can request their own personal data export via the settings UI

Export jobs are logged, processed asynchronously, and provide a secure expiring download link (time-limited, access-controlled)

Users can request account deletion; personal data is anonymised/removed while audit logs retain an anonymised actor reference

Company admins can initiate a staged company deletion (mark pending → delay window → cascade delete/anonymise)

Deletion operations are idempotent and safe to retry

User acceptance of terms and privacy policy is recorded with timestamp and version on signup and after policy updates

Admin UI shows: MFA policy control, per-user MFA status, export trigger, company deletion controls (with confirmation safeguard)

User settings show: MFA enable/disable, backup code view/regeneration, personal export request, account deletion request

All MFA actions (including backup code usage), export requests, deletion requests, and SSO config changes are captured in the audit log

⸻

Out of scope

Full SAML or OAuth provider implementation (architecture readiness only)
Enterprise SCIM provisioning
WebAuthn / hardware key support
Full legal policy management system or automated DPA generation
Cross-region data residency tooling

⸻

Tasks

Data model additions

Add new fields to users:
	•	mfa_enabled
	•	mfa_secret_encrypted
	•	mfa_backup_codes_hash
	•	mfa_enabled_at
	•	external_id
	•	identity_provider_id
	•	terms_accepted_at
	•	privacy_policy_version
	•	privacy_accepted_at

Create tables:
	•	identity_providers
	•	data_export_jobs
	•	data_deletion_requests

Update shared schema and storage interface accordingly.

⸻

MFA (TOTP) backend

Implement TOTP using otplib or speakeasy.
	•	Setup flow:
	•	generate secret
	•	display QR + manual code
	•	require verification before enabling
	•	generate backup codes (shown once, hashed at rest)
	•	Login flow:
	•	password validated first
	•	require TOTP if enabled
	•	allow backup code fallback (single-use)
	•	Management:
	•	enable / disable MFA (requires password + TOTP)
	•	regenerate backup codes
	•	Enforcement:
	•	enforce based on company mfa_policy at session creation
	•	block app access until setup is complete when required
	•	Audit:
	•	MFA enabled/disabled
	•	backup code used
	•	failed MFA attempts

⸻

SSO readiness layer

Refactor auth service to separate identity verification from session creation.
	•	Add identity provider CRUD
	•	Add stub SSO initiation + callback routes
	•	Extend user linkage (external_id, identity_provider_id)
	•	Maintain email as primary unique identifier

Ensure no regression to existing password login.

Audit:
	•	provider created/updated
	•	config changes

⸻

GDPR data export

Build async export job system.
	•	Admin: full company export
	•	User: personal data export

Include:
	•	user profile
	•	company profile
	•	metrics and values
	•	reports
	•	evidence metadata
	•	relevant audit records

Exclude:
	•	passwords
	•	API key raw values
	•	secrets

Output:
	•	JSON bundle (primary)
	•	optional CSV

Security:
	•	expiring download link
	•	access-controlled (user/company match)

Jobs must be retryable if interrupted.

Audit:
	•	request
	•	completion

⸻

GDPR data deletion

Implement controlled deletion flows.
	•	User deletion:
	•	remove/anonymise PII
	•	preserve audit logs with anonymised references
	•	Company deletion:
	•	mark pending
	•	optional delay window
	•	cascade delete/anonymise

Rules:
	•	no dangling references
	•	idempotent operations
	•	safe retries

Audit:
	•	request
	•	completion

⸻

Consent and policy tracking
	•	require acceptance on signup and policy updates
	•	store:
	•	terms_accepted_at
	•	privacy_policy_version
	•	privacy_accepted_at

⸻

Admin and user UI

Admin:
	•	MFA policy control
	•	user MFA status
	•	export trigger
	•	company deletion control (confirmation modal)

User:
	•	MFA setup/disable
	•	backup code management
	•	personal export request
	•	account deletion request

Super admin:
	•	MFA adoption rates
	•	export/delete activity

⸻

Relevant files

shared/schema.ts
server/storage.ts
server/routes.ts
server/auth.ts