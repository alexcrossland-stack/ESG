#50 - Task #49 Test Coverage Completion

## Objective

Task #49 ("First-time activation audit & fix") is merged, but its current test suite only covers unauthenticated baselines.

The goal of this task is to close the remaining validation gap by implementing the full regression suite required by Task #49:
- authenticated cross-tenant isolation
- RBAC enforcement
- session lifecycle enforcement
- malformed-payload validation expansion
- Playwright end-to-end coverage for key first-time activation journeys
- shared fixture/seed utilities and package scripts so the full suite can be run with one command

This task is about proving that the fixes from #49 actually hold under authenticated, role-based, and end-to-end conditions.

Do not duplicate seed logic between API and Playwright. One shared seed path only.

---

## Required operating approach

### Phase 1 — Review coverage and routes
- Review tests/api-security.test.ts
- Review protected and write routes in server/routes.ts
- Map each required test to real endpoints before implementing

### Phase 2 — Build seed utility
Create a deterministic shared fixture for tenants and users.

### Phase 3 — API test expansion
Add cross-tenant, RBAC, session lifecycle, and malformed payload coverage.

### Phase 4 — Playwright tests
Add E2E coverage for activation flows.

### Phase 5 — Standardize execution
Add npm scripts to run everything.

### Phase 6 — Coverage documentation
Add summary of what is covered and what is not.

---

## Done looks like

### API
- Authenticated users from Company A cannot access Company B resources (403/404 only)
- Viewer users cannot perform write actions (403)
- Admin-only and super-admin routes enforce role restrictions
- Logged-out or invalid sessions return 401
- Malformed payloads return 400 (never 500)

### Playwright
- Signup → login → logout works
- Onboarding minimum path + Quick Start works
- First metric entry works
- Dashboard first-run shows correct empty states + CTAs
- First report generation works
- Viewer restrictions enforced in UI and API

### Utilities
- Shared seed creates:
  - Tenant A: admin + viewer
  - Tenant B: admin + known resource IDs
- Reusable in API + Playwright
- Scripts:
  - test:api
  - test:e2e
  - test:all
- tests/COVERAGE.md documents coverage + gaps

---

## Tasks

### 1. Seed utility
Create:
tests/fixtures/seed.ts

Must:
- create two isolated tenants
- create required users and resources
- return auth sessions + IDs
- avoid collisions (unique prefix or cleanup)
- be usable by both API tests and Playwright setup

---

### 2. API tests — cross-tenant isolation

Extend tests/api-security.test.ts

Add suite:
- login as Tenant A admin
- attempt access to Tenant B resources:
  - metrics
  - data-entry
  - topics
  - reports
  - policies
  - questionnaires

Test:
- GET / PUT / DELETE / POST where applicable

Assertions:
- status = 403 or 404
- never 200
- no foreign tenant data in response
- no stack traces or raw DB output

---

### 3. API tests — RBAC

Add suite:
- login as viewer
- attempt:
  - POST /api/data-entry
  - PUT /api/metrics/:id/target
  - POST /api/reports/generate
  - PUT /api/company/settings
  - PUT /api/topics/:id
  - POST /api/actions

Also test:
- contributor blocked from admin/template routes
- admin blocked from super-admin routes

Assertions:
- status = 403
- admin equivalent succeeds

---

### 4. API tests — session lifecycle

Add suite:
- login → capture cookie
- verify access works
- logout
- reuse cookie across 5+ protected routes

Assertions:
- all return 401
- no 500s

Also test:
- invalid token → 401
- malformed token → 401

---

### 5. API tests — malformed payloads

Add suite covering:
- wrong types
- missing fields
- invalid values
- oversized payloads

Endpoints:
- POST /api/data-entry
- POST /api/auth/register
- PUT /api/metrics/:id/target
- POST /api/reports/generate

Assertions:
- status = 400
- safe error message
- never 500

---

### 6. Playwright setup

Install Playwright and create:
- playwright.config.ts
- global setup using seed utility
- auth state reuse

Target:
http://localhost:5000

---

### 7. Playwright tests

Create in tests/e2e/

auth.spec.ts
- signup, login, logout
- assert no raw errors

onboarding.spec.ts
- complete minimum onboarding
- Quick Start appears + can dismiss
- no 500s

metric-entry.spec.ts
- add first metric
- assert success + visible data

dashboard.spec.ts
- verify empty states
- verify CTA navigation

reports.spec.ts
- generate report
- assert success state

viewer-restrictions.spec.ts
- login as viewer
- assert no write UI
- direct fetch POST returns 403

---

### 8. Scripts + coverage

Add to package.json:
- test:api
- test:e2e
- test:all

Create:
tests/COVERAGE.md

Include:
- what each suite covers
- what is not covered (MFA, step-up auth, email, etc.)

---

## Route references

Review before implementing:
- server/routes.ts:133-330
- server/routes.ts:827-1090
- server/routes.ts:1263-1300
- server/routes.ts:1769-1870
- server/routes.ts:1898-2030
- server/routes.ts:2937-2960
- server/routes.ts:3347-3440
- server/routes.ts:4422-4445

Also:
- shared/schema.ts:991-1045

---

## Deliverables

1. New/updated test files
2. Shared seed utility
3. Updated package.json scripts
4. tests/COVERAGE.md
5. Short summary:
   - what is covered
   - what routes were tested
   - remaining gaps

---

## Quality bar

Tests must assert:
- correct status codes
- safe responses
- correct role/tenant enforcement
- correct navigation/UX outcomes

If a route cannot be tested, document it in COVERAGE.md.