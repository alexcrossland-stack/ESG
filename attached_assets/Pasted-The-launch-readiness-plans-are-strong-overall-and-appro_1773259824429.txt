The launch-readiness plans are strong overall and approved in principle. Please make the following refinements before implementation starts:

General
	1.	Renumber the tasks cleanly so there is no duplicate “#11” and no missing task number.

10 — Environment & Deployment Configuration
	2.	Implement CSP carefully and verify it does not break charts, file flows, routing, or authenticated app behaviour in production.
	3.	Add explicit JSON body size limits and file upload size limits as part of production hardening.
	4.	Define cookie/session expiry behaviour clearly, including secure cookie handling, max age, and trust proxy behaviour.

11 — Automated Database Backup System
	5.	Before implementation, verify whether pg_dump is actually available and supported in the deployment environment.
	6.	Be explicit about where actual backup files are stored and whether that storage is durable across restarts and deployments. Metadata alone is not sufficient.
	7.	Add checksum and basic backup verification so backups are validated as usable, not just created.
	8.	Manual backup should enqueue a tracked job, not run inline.

12 — Security Hardening Pass
	9.	For file uploads and imports, do not rely only on extension and client-declared MIME type. Where practical, also verify actual file type or file signature.
	10.	Apply upload validation only to upload/import routes. For report downloads, focus on access control, correct content type, and safe file naming.
	11.	Use IP rate limits as a baseline, but apply company-level or user-level limits for authenticated expensive routes such as report generation, imports, and AI operations.
	12.	Zod validation should include maximum lengths, array limits, enum enforcement, and other payload bounds — not only structural validation.
	13.	Produce a short written summary of the company isolation audit listing any risky routes found and confirming that companyId for company-scoped access always comes from session/auth context.
	14.	The /api/admin/security-audit output should separate categories such as failed logins, permission denials, rate-limit hits, rejected uploads, and 5xx errors.

13 — Email Service Integration
	15.	Use a consistent token model for invitations and password resets.
	16.	Store invitation and password reset tokens hashed at rest.
	17.	Forgot-password responses must not reveal whether an email exists.
	18.	Add resend/rate-limit controls for invitations and reset emails.

14 — Stripe Subscription Billing
	19.	Define exact counting rules for Free-plan limits so usage is predictable and fair.
	20.	Verify Stripe webhook signatures properly and update billing state only from validated webhook events.
	21.	Define downgrade behaviour clearly when a company exceeds Free limits after cancellation or downgrade.
	22.	Clarify whether trial support is actually in scope; if not, remove trial handling from the plan.

15 — Platform Health Monitoring Expansion
	23.	Avoid storing sensitive request data, import contents, or secrets in health events.
	24.	Add sensible retention or summarisation for high-volume health logs.
	25.	Make the daily health summary email configurable and super-admin only.

16 — Beta Launch Readiness
	26.	Restrict demo reset to super-admin only with explicit confirmation and hard-scope it to the demo company.
	27.	Ensure React error boundary events are logged as well as displayed.
	28.	Make the first-run dashboard guidance concrete, with 3 clear next actions rather than generic messaging.

Once these refinements are included, proceed with 10, 12, 13, 14, 15, and 16. Revise 11 before implementation if backup durability and recoverability are still unclear.
