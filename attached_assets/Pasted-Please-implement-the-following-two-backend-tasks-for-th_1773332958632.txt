Please implement the following two backend tasks for the ESG Manager platform as a clean, additive integration layer for the external AI Agent Service.

IMPORTANT IMPLEMENTATION RULES
- Do not modify existing business logic or existing /api routes except where explicitly required to register the new route group or trigger critical health webhooks.
- Keep this additive and isolated.
- Put all new agent integration routes in a new file: server/agent-routes.ts
- Put all new agent authentication logic in a new file: server/agent-auth.ts
- Put all new webhook logic in a new file: server/webhooks.ts
- Extend shared/schema.ts and server/storage.ts only as needed for the new schema and storage methods.
- Reuse existing storage methods where possible for platform health events, support requests, audit logs, users, companies, settings, onboarding, etc.
- Follow existing Drizzle, Zod, and TypeScript patterns already used in the codebase.
- Do not expose API key hashes in any response.
- Return plaintext API keys only once at creation time.

==================================================
TASK #17 — AI AGENT SCHEMA & STORAGE LAYER
==================================================

GOAL
Add the database foundation required for AI Agent Service integration. This includes:
- machine-to-machine API keys
- agent execution audit tables
- chat session/message tables for in-app support chat

OUT OF SCOPE
- Routes
- Authentication middleware
- Webhook dispatch logic
- Changes to existing tables
- Changes to existing business logic

DONE LOOKS LIKE
- Six new tables exist:
  - agent_api_keys
  - agent_runs
  - agent_actions
  - agent_escalations
  - chat_sessions
  - chat_messages
- shared/schema.ts contains full Drizzle definitions, insert schemas, select types, and insert types
- server/storage.ts IStorage includes CRUD methods for all six tables
- DatabaseStorage implements all methods
- Schema is pushed successfully to the live database

1. ADD ENUMS AND TABLES IN shared/schema.ts

Add an agentTypeEnum with these values:
- technical_agent
- customer_success_agent
- esg_specialist_agent
- marketing_agent
- master_orchestrator

Add any additional enums needed for:
- chat session status: open, closed
- chat message role: user, agent, system
- escalation priority/status if needed
- run/action status if needed

Add these six tables:

A) agent_api_keys
Columns:
- id (uuid/varchar, PK)
- agent_type (agentTypeEnum, not null)
- label (text, not null)
- key_hash (text, not null, unique)
- key_prefix (text, not null)
- scopes (jsonb, not null) // array of allowed scope strings, e.g. ["internal:*"] or ["internal:health", "internal:chat"]
- company_id (varchar, nullable) // null = platform-level key
- last_used_at (timestamp, nullable)
- expires_at (timestamp, nullable)
- revoked_at (timestamp, nullable)
- created_at (timestamp, default now, not null)

B) agent_runs
Columns:
- id (uuid/varchar, PK)
- agent_name (text, not null)
- agent_type (agentTypeEnum, not null)
- trigger_type (text, not null)
- input_summary (text, nullable)
- output_summary (text, nullable)
- status (text, not null)
- duration_ms (integer, nullable)
- company_id (varchar, nullable)
- user_id (varchar, nullable)
- created_at (timestamp, default now, not null)

C) agent_actions
Columns:
- id (uuid/varchar, PK)
- run_id (varchar, not null, FK -> agent_runs.id, cascade delete if appropriate)
- action_type (text, not null)
- entity_type (text, nullable)
- entity_id (varchar, nullable)
- input_summary (text, nullable)
- output_summary (text, nullable)
- status (text, not null)
- error (text, nullable)
- created_at (timestamp, default now, not null)

D) agent_escalations
Columns:
- id (uuid/varchar, PK)
- run_id (varchar, nullable, FK -> agent_runs.id)
- agent_name (text, not null)
- escalation_type (text, not null)
- priority (text, not null)
- summary (text, not null)
- company_id (varchar, nullable)
- user_id (varchar, nullable)
- related_entity_type (text, nullable)
- related_entity_id (varchar, nullable)
- status (text, not null)
- resolved_at (timestamp, nullable)
- created_at (timestamp, default now, not null)

E) chat_sessions
Columns:
- id (uuid/varchar, PK)
- company_id (varchar, nullable)
- user_id (varchar, nullable)
- agent_type (agentTypeEnum, nullable)
- title (text, nullable)
- status (text, not null) // open or closed
- created_at (timestamp, default now, not null)
- updated_at (timestamp, default now, not null)

F) chat_messages
Columns:
- id (uuid/varchar, PK)
- session_id (varchar, not null, FK -> chat_sessions.id, cascade delete if appropriate)
- role (text, not null) // user, agent, system
- content (text, not null)
- metadata (jsonb, nullable) // confidence, sources, agent reasoning summary, etc.
- created_at (timestamp, default now, not null)

2. ADD INSERT SCHEMAS AND TYPES

For each new table:
- add createInsertSchema(...)
- omit id and created_at where appropriate
- for chat_sessions also omit updated_at
- export:
  - insert type
  - select type

Follow the exact file pattern already used elsewhere in shared/schema.ts.

3. EXTEND server/storage.ts

Add to IStorage:

Agent API key methods
- createAgentApiKey(...)
- getAgentApiKeyByHash(hash: string)
- listAgentApiKeys()
- revokeAgentApiKey(id: string)
- updateAgentApiKeyLastUsed(id: string)

Agent run/action/escalation methods
- createAgentRun(...)
- updateAgentRun(id: string, updates)
- createAgentAction(...)
- createAgentEscalation(...)
- listAgentEscalations(filters?)

Chat methods
- createChatSession(...)
- getChatSession(id: string)
- listChatSessions(filters?)
- createChatMessage(...)
- getChatMessages(sessionId: string)

Implement all methods in DatabaseStorage using Drizzle and existing storage patterns.

4. PUSH SCHEMA
Run:
- npm run db:push

Verify all six tables exist.

==================================================
TASK #18 — AI AGENT INTEGRATION LAYER
==================================================

GOAL
Build the full integration surface that allows a separate Python AI Agent Service to securely interact with the ESG Manager platform.

This includes:
- persistent M2M API key authentication
- internal /api/internal/agent/* route group
- internal /api/internal/chat/* route group
- public /health endpoint
- outbound webhook dispatch for critical health events
- route registration via one new file

DONE LOOKS LIKE
- POST /api/internal/agent/keys generates API key and returns plaintext once
- GET /api/internal/agent/keys lists non-secret metadata
- DELETE /api/internal/agent/keys/:id revokes a key
- All /api/internal/agent/* and /api/internal/chat/* routes accept X-Agent-API-Key and enforce scope-based access
- GET /api/internal/agent/company/:companyId returns structured company context
- GET /api/internal/agent/user/:userId returns user context
- GET /api/internal/agent/health returns platform health summary
- POST /api/internal/agent/escalations stores escalation and can optionally create support ticket
- GET /api/internal/agent/escalations lists escalations
- GET /api/internal/agent/knowledge supports keyword/category search across in-memory knowledge sources
- GET /api/internal/agent/events returns recent platform health events
- GET /api/internal/agent/support-tickets returns support requests
- GET /api/internal/agent/audit-logs/:companyId returns recent audit logs
- POST /api/internal/agent/runs creates a run
- PATCH /api/internal/agent/runs/:id updates run
- POST /api/internal/agent/runs/:runId/actions creates an action
- GET /health is public and returns app/db/scheduler/timestamp with 200 or 503
- Critical health events trigger best-effort POST to AGENT_WEBHOOK_URL
- POST /api/internal/chat/sessions creates session
- GET /api/internal/chat/sessions lists sessions
- GET /api/internal/chat/sessions/:id/messages returns message history
- POST /api/internal/chat/sessions/:id/messages stores user message, forwards to AGENT_SERVICE_URL if configured, stores agent reply, returns reply
- .env.example documents AGENT_WEBHOOK_URL and AGENT_SERVICE_URL
- server/routes.ts only changes to register registerAgentRoutes(app) at the end and to invoke webhook dispatch asynchronously after critical health events are created where applicable

OUT OF SCOPE
- frontend chat widget
- AI logic inside the Node platform
- modifying existing /api routes
- webhook retry queue
- company-specific key scoping beyond nullable company_id
- agent-side prompting/model logic

1. CREATE server/agent-auth.ts

Add:
A) generateAgentApiKey()
Requirements:
- Generate secure plaintext key
- Return:
  - plaintext
  - hash
  - prefix
- Store only hash in DB
- Prefix should be safe for display, e.g. first few chars plus marker

B) requireAgentAuth middleware
Requirements:
- Read X-Agent-API-Key header
- Hash the provided key
- Look up in agent_api_keys by hash
- Reject if:
  - missing
  - not found
  - revoked
  - expired
- Attach agent context to request, including:
  - key id
  - agent type
  - scopes
  - company_id if present
- Update last_used_at asynchronously or inline safely

C) requireAgentScope(scope: string) middleware
Requirements:
- Support scopes like:
  - internal:*
  - internal:health
  - internal:company
  - internal:user
  - internal:events
  - internal:escalations
  - internal:runs
  - internal:chat
  - internal:knowledge
- Authorize if exact scope exists or internal:* exists

Also support frontend user chat compatibility where specified later:
- user-authenticated requests to chat routes may use existing session or Bearer token auth where appropriate

2. CREATE server/agent-routes.ts

Export:
- registerAgentRoutes(app: Express)

Put all new routes here.

3. AGENT KEY MANAGEMENT ROUTES

Add:
- POST /api/internal/agent/keys
- GET /api/internal/agent/keys
- DELETE /api/internal/agent/keys/:id

Requirements:
- Require normal user auth plus admin role
- POST accepts:
  - agentType
  - label
  - scopes
  - optional expiresAt
  - optional companyId
- Generate plaintext key using generateAgentApiKey()
- Store hash/prefix/metadata
- Return plaintext only once on creation
- GET returns only safe metadata, never hash
- DELETE sets revoked_at

4. COMPANY, USER, AND HEALTH CONTEXT ROUTES

Add:
- GET /api/internal/agent/company/:companyId
- GET /api/internal/agent/user/:userId
- GET /api/internal/agent/health

Requirements:

Company endpoint
- Require agent auth + internal:company
- Return structured company context:
  - company profile
  - plan tier/status
  - company settings
  - onboarding state/status
  - selected modules/metrics if available

User endpoint
- Require agent auth + internal:user or internal:company if you decide to allow both
- Return:
  - user profile
  - role
  - permissions derived from role
  - company context

Health endpoint
- Require agent auth + internal:health
- Return:
  - scheduler status
  - last 24h error counts by type/severity
  - recent error events
- Reuse existing storage methods where possible

5. KNOWLEDGE RETRIEVAL ROUTE

Add:
- GET /api/internal/agent/knowledge?q=&category=

Requirements:
- Require agent auth + internal:knowledge
- Read-only in-memory search across:
  - policy templates from server/policy-templates.ts
  - 28 default metric names/descriptions/help text
  - benchmarks from server/benchmarks.ts
  - compliance framework names/descriptions
- Return array of:
  - source
  - category
  - title if available
  - text
  - metadata if useful
- Do not create a new table
- Do not call an AI model
- Keep it deterministic/simple

6. EVENTS, SUPPORT TICKETS, AND AUDIT LOG ROUTES

Add:
- GET /api/internal/agent/events?severity=&type=&limit=
- GET /api/internal/agent/support-tickets?status=&limit=
- GET /api/internal/agent/audit-logs/:companyId?limit=

Requirements:
- Require agent auth + internal:events
- Use existing storage methods where possible
- Return recent records with sensible limits and defaults

7. ESCALATION ROUTES

Add:
- POST /api/internal/agent/escalations
- GET /api/internal/agent/escalations

Requirements:
- POST requires agent auth + internal:escalations
- Store escalation in agent_escalations
- Accept body fields such as:
  - runId
  - agentName
  - escalationType
  - priority
  - summary
  - companyId
  - userId
  - relatedEntityType
  - relatedEntityId
  - createSupportTicket
- If createSupportTicket is true:
  - also create a support_requests row
  - include linkage in notes/details if possible
- GET lists escalations and supports filters like:
  - status
  - companyId
  - limit

8. AGENT RUN AND ACTION RECORDING ROUTES

Add:
- POST /api/internal/agent/runs
- PATCH /api/internal/agent/runs/:id
- POST /api/internal/agent/runs/:runId/actions

Requirements:
- Require agent auth + internal:runs
- POST /runs creates a run start and returns run id
- PATCH /runs/:id updates output_summary, status, duration_ms, etc.
- POST /runs/:runId/actions records an action row linked to the run

9. PUBLIC /health ENDPOINT

Add:
- GET /health

Requirements:
- Public, no auth
- Register before auth-protected route logic as appropriate
- Run a lightweight DB connectivity check, e.g. SELECT 1
- Check scheduler status using existing scheduler state helper if available
- Return:
  {
    "status": "ok" | "degraded",
    "db": "connected" | "error",
    "scheduler": "running" | "stopped",
    "timestamp": "..."
  }
- Return HTTP 200 if healthy, 503 if degraded

10. CREATE server/webhooks.ts

Add:
- dispatchCriticalHealthEvent(event)

Requirements:
- Check AGENT_WEBHOOK_URL
- If configured, do a best-effort fetch POST
- JSON body should include:
  - eventType
  - severity
  - message
  - details
  - timestamp
- Catch all errors
- Log failures but never throw
- This must never block the main caller

11. TRIGGER WEBHOOKS FOR CRITICAL HEALTH EVENTS

Where the platform currently creates platform_health_events with severity=error and event_type in:
- server_error
- job_failure
- ai_failure
- import_failure

after the storage call succeeds, asynchronously call dispatchCriticalHealthEvent(...)

IMPORTANT:
- Do not modify createPlatformHealthEvent storage method itself
- Do not break existing flows
- Best-effort only

12. INTERNAL CHAT ROUTES

Add:
- POST /api/internal/chat/sessions
- GET /api/internal/chat/sessions
- GET /api/internal/chat/sessions/:id/messages
- POST /api/internal/chat/sessions/:id/messages

Requirements:

Auth model
- These routes should support:
  - X-Agent-API-Key for agent service access
  - existing user auth/session/Bearer token for frontend chat widget compatibility where appropriate

POST /api/internal/chat/sessions
- Create a chat session
- Accept:
  - companyId
  - userId
  - agentType
  - title
- Return created session

GET /api/internal/chat/sessions
- List sessions by current user and/or company
- Support filters as needed

GET /api/internal/chat/sessions/:id/messages
- Return full ordered message history for the session

POST /api/internal/chat/sessions/:id/messages
Flow:
1. Store user message in chat_messages with role = user
2. Load session and recent message history
3. If AGENT_SERVICE_URL is configured:
   - POST to ${AGENT_SERVICE_URL}/chat
   - send session context + message history + latest message
4. Parse external response
5. Store returned agent message in chat_messages with role = agent
6. Return agent message immediately

If AGENT_SERVICE_URL is not configured:
- store and return a placeholder agent response, e.g. “Agent service not configured yet.”

Message metadata
- allow metadata jsonb for confidence, sources, model info, etc.

13. UPDATE .env.example

Add and document:
- AGENT_WEBHOOK_URL=
- AGENT_SERVICE_URL=

Include short comments explaining purpose.

14. WIRE ROUTES INTO MAIN APP

At the end of server/routes.ts:
- import registerAgentRoutes from server/agent-routes.ts
- call registerAgentRoutes(app) once, after existing routes

Keep modifications to existing files minimal.

15. IMPLEMENTATION QUALITY REQUIREMENTS

- Use TypeScript throughout
- Validate request bodies with Zod where appropriate
- Reuse existing auth helpers for admin-only user routes
- Reuse existing storage layer instead of direct DB access in routes where possible
- Keep responses structured and consistent
- Add clear error handling
- Keep all new code production-safe
- Do not expose secrets
- Do not break existing tests or existing routes

16. AFTER IMPLEMENTATION

Please provide:
1. A summary of all files created/changed
2. The exact new database tables and enums added
3. The new storage methods added
4. The full list of new routes added
5. Any environment variables added
6. Any assumptions made
7. Any security considerations
8. Whether npm run db:push succeeded
9. Any follow-up tasks needed to connect the external Python AI Agent Service